[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simpson-common"
version = "0.1.0"
dependencies = [
    "pydantic>=2.6.0",
    "pydantic-settings>=2.2.0",
    "structlog>=24.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["simpson_common"]
