[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simpson-testing"
version = "0.1.0"
dependencies = [
    "pytest>=8.1.0",
    "simpson-domain",
    "simpson-provenance",
]

[tool.hatch.build.targets.wheel]
packages = ["simpson_testing"]
