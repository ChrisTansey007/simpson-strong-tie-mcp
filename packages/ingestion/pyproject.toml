[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simpson-ingestion"
version = "0.1.0"
dependencies = [
    "pydantic>=2.6.0",
    "simpson-domain",
    "simpson-provenance",
    "simpson-storage",
    "simpson-persistence",
]

[tool.hatch.build.targets.wheel]
packages = ["simpson_ingestion"]
