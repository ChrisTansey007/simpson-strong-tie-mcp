[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simpson-domain"
version = "0.1.0"
dependencies = [
    "pydantic>=2.6.0",
]

[tool.hatch.build.targets.wheel]
packages = ["simpson_domain"]
